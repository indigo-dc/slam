
states:

  - !com.agreemount.bean.document.state.State
    id: documentType
    label: documentType
    values:
      - !com.agreemount.bean.document.state.StateValue
        id: sla
        label: SLA

  - !com.agreemount.bean.document.state.State
    id: mainState
    label: statusMain
    values:
      - !com.agreemount.bean.document.state.StateValue
        id: draft
        label: draft
      - !com.agreemount.bean.document.state.StateValue
        id: signed
        label: signed
      - !com.agreemount.bean.document.state.StateValue
        id: sent
        label: sent
      - !com.agreemount.bean.document.state.StateValue
        id: rejected
        label: rejected
      - !com.agreemount.bean.document.state.StateValue
        id: deleted
        label: deleted

  - !com.agreemount.bean.document.state.State
    id: sideState
    label: sideState
    values:
      - !com.agreemount.bean.document.state.StateValue
        id: customer
        label: customer
      - !com.agreemount.bean.document.state.StateValue
        id: provider
        label: provider

  - !com.agreemount.bean.document.state.State
    id: serviceType
    label: serviceType
    values:
      - !com.agreemount.bean.document.state.StateValue
        id: computing
        label: computing
      - !com.agreemount.bean.document.state.StateValue
        id: storage
        label: storage

