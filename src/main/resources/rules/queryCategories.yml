queryCategories:

  - !com.agreemount.bean.query.QueryCategory
    id: default
    label: Default query category

  - !com.agreemount.bean.query.QueryCategory
    id: providerDashboard
    label: query for provider

  - !com.agreemount.bean.query.QueryCategory
    id: adminDashboard
    label: query for provider

  - !com.agreemount.bean.query.QueryCategory
    id: userDashboard
    label: query for user
